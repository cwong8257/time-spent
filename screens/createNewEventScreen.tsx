import React, {Component} from 'react';
import {SafeAreaView, ScrollView} from 'react-native';

export default class CreateNewEventScreen extends Component {
  state = {};

  render() {
    return (
      <SafeAreaView>
        <ScrollView contentInsetAdjustmentBehavior="automatic" />
      </SafeAreaView>
    );
  }
}
